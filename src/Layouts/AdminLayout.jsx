import auth from "../firebase/firebase.init";

const AdminLayout = () => {
    console.log(auth);
    return (
        <div>
            this admin Layouts
        </div>
    );
};

export default AdminLayout;